class NetworkAutomationLocators:
    DISCOVER_SWITCHES = "Discover Switches"  # page.get_by_role("button", name="Discover Switches").click()
    ACKNOWLEDGEMENT_CHECKBOX = "confirm-modal-confirm-check"
    ACKNOWLEDGEMENT_CHECKBOX_TEXT = "I acknowledge the implications of the current action."
    # confirm-modal-confirm-check is present in common_locators.py
    # confirm-modal-primary-button is present in common_locators.py

    # we will add more once Network Automation is enabled
